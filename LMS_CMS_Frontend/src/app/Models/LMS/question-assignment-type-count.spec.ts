import { QuestionAssignmentTypeCount } from './question-assignment-type-count';

describe('QuestionAssignmentTypeCount', () => {
  it('should create an instance', () => {
    expect(new QuestionAssignmentTypeCount()).toBeTruthy();
  });
});
