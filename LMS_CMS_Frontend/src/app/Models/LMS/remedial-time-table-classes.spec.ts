import { RemedialTimeTableClasses } from './remedial-time-table-classes';

describe('RemedialTimeTableClasses', () => {
  it('should create an instance', () => {
    expect(new RemedialTimeTableClasses()).toBeTruthy();
  });
});
