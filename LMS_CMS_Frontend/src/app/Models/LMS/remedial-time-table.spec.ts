import { RemedialTimeTable } from './remedial-time-table';

describe('RemedialTimeTable', () => {
  it('should create an instance', () => {
    expect(new RemedialTimeTable()).toBeTruthy();
  });
});
