import { CertificatesIssuer } from './certificates-issuer';

describe('CertificatesIssuer', () => {
  it('should create an instance', () => {
    expect(new CertificatesIssuer()).toBeTruthy();
  });
});
