import { MaintenanceEmployees } from './maintenance-employees';

describe('MaintenanceEmployees', () => {
  it('should create an instance', () => {
    expect(new MaintenanceEmployees()).toBeTruthy();
  });
});
