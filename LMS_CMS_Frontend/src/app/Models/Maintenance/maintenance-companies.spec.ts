import { MaintenanceCompanies } from './maintenance-companies';

describe('MaintenanceCompanies', () => {
  it('should create an instance', () => {
    expect(new MaintenanceCompanies()).toBeTruthy();
  });
});
